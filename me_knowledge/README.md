# Local Knowledge Workspace

This directory is reserved for local knowledge artifacts used by `knowledge.*` tools.

- `notes/`: local research notes (not published in this repository).
- `sim_changes/`: local simulation research notes (not published in this repository).
- `lancedb/`: local vector/FTS index storage.

Keep local content in these folders as needed; only placeholder files are tracked.
